require 'spec_helper'

describe TopTechnoCli do
  it 'has a version number' do
    expect(TopTechnoCli::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
